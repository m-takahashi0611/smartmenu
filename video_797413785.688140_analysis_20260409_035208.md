1. お弁当モードの設定は保存されていません。
「お弁当モードの設定を保存する」ボタンをタップすると、画面下部に「保存しました」というメッセージが表示されますが、元の画面には自動で遷移しません。その後、手動で「戻る」ボタンを押して前の画面に戻り、再度「お弁当モード」を開くと、先ほどONにした設定や選択した項目がすべてリセットされ、元の状態（OFF）に戻ってしまっています。

2. スイッチをONにした時、白い丸は右側に移動します。
しかし、右端までは移動しきっておらず、スイッチの背景（緑色）の右端と白い丸の間に少し隙間が残っている状態です。